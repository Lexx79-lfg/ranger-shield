import { CheckCircle2 } from 'lucide-react';
import type { Recommendation } from '../types/assessment';
export function RecommendationCard({recommendation}:{recommendation:Recommendation}){const badge={High:'bg-red-500/15 text-red-200 border-red-500/30',Medium:'bg-amber-500/15 text-amber-200 border-amber-500/30',Low:'bg-emerald-500/15 text-emerald-200 border-emerald-500/30'}[recommendation.priority];return <article className="rounded-lg border border-slate-800 bg-slate-900 p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-sm text-blue-200">{recommendation.category}</p><h3 className="mt-1 text-lg font-semibold">{recommendation.title}</h3></div><span className={'rounded-full border px-3 py-1 text-sm '+badge}>{recommendation.priority}</span></div><p className="mt-3 text-slate-300">{recommendation.explanation}</p><p className="mt-3 text-sm text-slate-400"><strong className="text-slate-200">Action:</strong> {recommendation.recommendedAction}</p><div className="mt-4 grid gap-3 md:grid-cols-2"><div><p className="text-sm font-semibold">Evidence to collect</p><ul className="mt-2 space-y-1 text-sm text-slate-400">{recommendation.evidenceToCollect.map(e=><li key={e} className="flex gap-2"><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-blue-300"/>{e}</li>)}</ul></div><div><p className="text-sm font-semibold">Remediation steps</p><ol className="mt-2 list-decimal space-y-1 pl-5 text-sm text-slate-400">{recommendation.remediationSteps.map(s=><li key={s}>{s}</li>)}</ol></div></div></article>}
