import type { CategoryScore } from '../types/assessment';
export function CategoryScoreCard({item}:{item:CategoryScore}){return <div className="rounded-lg border border-slate-800 bg-slate-900 p-4"><div className="flex items-start justify-between gap-3"><h3 className="font-semibold">{item.category}</h3><span className="rounded-full bg-slate-800 px-2 py-1 text-sm">{item.score}%</span></div><p className="mt-2 text-sm text-slate-400">{item.answered}/{item.total} answered · {item.missingCount} safeguard findings</p><div className="mt-3 h-2 rounded-full bg-slate-800"><div className="h-2 rounded-full bg-blue-500" style={{width:item.score+'%'}}/></div></div>}
